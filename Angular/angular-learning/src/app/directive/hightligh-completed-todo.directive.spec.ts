import { HightlighCompletedTodoDirective } from './hightligh-completed-todo.directive';

describe('HightlighCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlighCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
